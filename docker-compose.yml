version: '3'
services:
  backend:
    container_name: Wallet-nest-backend
    build: .
    ports:
      - 1078:3000
    restart: always
    networks:
      - xplor-network

networks:
  xplor-network:
    external: true
